{
    "Logging": {
        "IncludeScopes": false,
        "LogLevel": {
            "Default": "Debug",
            "Microsoft": "Warning",
            "Microsoft.Hosting.Lifetime": "Warning"
        }
    },
    "AllowedHosts": "*",
    "NLog": {
        "targets": {
            "logConsole": {
                "type": "ColoredConsole",
                "layout": "${uppercase:${level}}|${logger}|${message} ${exception:format=tostring}"
            },
            "high_logFile": {
                "type": "file",
                "name": "file",
                "fileName": "logs/crypto_high_${shortdate}.log",
                "layout": "${longdate}|[${threadid}]|${uppercase:${level}}|${logger}|${message} ${exception:format=tostring}"
            },
            "logFile": {
                "type": "file",
                "name": "file",
                "fileName": "logs/crypto_${shortdate}.log",
                "layout": "${longdate}|[${threadid}]|${uppercase:${level}}|${logger}|${message} ${exception:format=tostring}"
            }
        },
        "rules": [
            {
                "logger": "*",
                "minLevel": "Info",
                "writeTo": "logConsole"
            },
            {
                "logger": "*",
                "minLevel": "Info",
                "writeTo": "logFile"
            },
            {
                "logger": "*",
                "minLevel": "Trace",
                "writeTo": "high_logFile"
            }
        ]
    }
}